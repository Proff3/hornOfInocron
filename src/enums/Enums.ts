/**
 * Enum валют
 */
export const enum ExchangeEnum {
    EUR,
    USD,
    CNY,
}

/**
 * Enum ресурса данных в бд
 * Используется для работы с бд
 */
export const enum ResourseEnum {
    EXCHANGES = "exchanges",
    MOVIE = "movie",
    PHRASE = "phrase",
    WEATHER = "weather",
}
